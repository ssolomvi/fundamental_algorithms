#define _CRT_SECURE_NO_WARNINGS

#ifndef HEADER_6
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
#include <math.h>

#define is_ident(c) (((c) >= '0' && (c) <= '9'))
#define is_operator(c) ((c) == '+' || (c) == '-' || (c) == '/' || (c) == '*' || (c) == '%' || (c) == '^')

#endif // HEADER_6